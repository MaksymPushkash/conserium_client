# npm audit snapshot - 2026-05-13

Command: `npm audit --audit-level=moderate`

Result: failed with 2 vulnerabilities.

- `next` high severity advisories affecting versions `9.3.4-canary.0 - 16.3.0-canary.5`; npm reports `npm audit fix` is available.
- `postcss <8.5.10` moderate severity advisory via `next/node_modules/postcss`; npm reports `npm audit fix` is available.

This snapshot is documentation only. Dependency remediation is intentionally kept separate from the maintainability refactor.
